# Optional: Clone Official NVIDIA Repos

```bash
git clone https://github.com/NVIDIA/gpu-operator.git
git clone https://github.com/NVIDIA/k8s-device-plugin.git
git clone https://github.com/NVIDIA/DCGM.git
git clone https://github.com/NVIDIA/dcgm-exporter.git
```

## How to use these with this sprint repo

- Use `nca-aiio-study-labs` for speed and exam alignment.
- Use NVIDIA repos for real deployment patterns, flags, and manifests.
- Keep notes on what each repo solves in production.
